// import { Router } from "express";
// const userRouter = Router()

// POST /jobs — Create job application
// Fields: company, position, status, appliedDate, location, jobType, notes

// GET /jobs — Get user's jobs

// Optional query: ?status=applied&sort=desc

// GET /jobs/:id — Get job by ID

// PUT /jobs/:id — Update job

// DELETE /jobs/:id — Delete job

// 📊 Filtering & Sorting
// userRouter.get()